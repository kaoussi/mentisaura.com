---
import Layout from '../components/Layout.astro';
import Navigation from '../components/Navigation';
import Footer from '../components/Footer';
import '../styles/globals.css';
---

<Layout
  title="404 - Page Not Found - Mentis Aura"
  description="The page you're looking for doesn't exist or is under development."
>
  <Navigation client:load />

  <main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-50 via-slate-100 to-slate-200 relative overflow-hidden">
    <div class="absolute inset-0 opacity-5">
      <div class="absolute inset-0" style="background-image: radial-gradient(circle, #334155 1px, transparent 1px); background-size: 30px 30px;"></div>
    </div>
    <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 text-center py-20 relative z-10">
      <!-- 404 -->
      <h1 class="text-9xl font-bold bg-gradient-to-br from-gray-300 to-gray-400 bg-clip-text text-transparent mb-8 font-playfair">404</h1>

      <!-- Message -->
      <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6 font-playfair">Page Not Found</h2>
      <p class="text-xl md:text-2xl text-gray-600 mb-10">
        The page you're looking for doesn't exist—or hasn't been built yet.
      </p>

      <!-- Development Notice -->
      <div class="bg-gradient-to-br from-yellow-50 to-yellow-100 border-2 border-yellow-300 rounded-2xl p-8 mb-10 shadow-lg">
        <div class="flex items-center justify-center gap-3 mb-4">
          <div class="w-10 h-10 bg-yellow-600 rounded-lg flex items-center justify-center">
            <span class="text-white text-xl">⚠️</span>
          </div>
          <p class="text-gray-900 font-bold text-lg">Website Under Development</p>
        </div>
        <p class="text-gray-700 text-lg">
          This website is under continuous development. Some pages may not be available yet. We're building thoughtfully, not quickly.
        </p>
      </div>

      <!-- Navigation Suggestions -->
      <div class="mb-12">
        <p class="text-gray-700 text-lg mb-6 font-semibold">Perhaps you meant to visit:</p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="/"
            class="inline-flex items-center justify-center bg-gradient-to-r from-slate-800 to-slate-900 text-white px-8 py-4 rounded-xl hover:scale-105 transition-all font-semibold shadow-xl"
          >
            Homepage
          </a>
          <a
            href="/book"
            class="inline-flex items-center justify-center bg-blue-600 text-white px-8 py-4 rounded-xl hover:scale-105 transition-all font-semibold shadow-xl"
          >
            View The Book
          </a>
          <a
            href="/contact"
            class="inline-flex items-center justify-center border-2 border-gray-900 text-gray-900 px-8 py-4 rounded-xl hover:bg-gray-900 hover:text-white transition-all font-semibold shadow-lg"
          >
            Contact Us
          </a>
        </div>
      </div>

      <!-- Philosophical Note -->
      <div class="mt-16 pt-8 border-t-2 border-gray-300">
        <p class="text-gray-500 italic text-xl font-playfair">
          "Not all who wander are lost. But this page definitely is."
        </p>
      </div>
    </div>
  </main>

  <Footer client:load />
</Layout>
